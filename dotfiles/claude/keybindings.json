{
  "$schema": "https://www.schemastore.org/claude-code-keybindings.json",
  "bindings": [
    {
      "context": "Chat",
      "bindings": {
        "ctrl+f": "chat:submit /flake-check",
        "ctrl+g": "chat:submit /glog 5",
        "ctrl+o": "chat:submit /sessions"
      }
    }
  ]
}
