#compdef anki-sync-failures.py
_arguments \
  '--dry-run[Evaluate without mutating the filesystem]' \
  '--threshold[Threshold metric defining a leech (default: 0.8)]' \
  '--min-reviews[Minimum review count (default: 3)]' \
  '(-h --help)'{-h,--help}'[Display usage instructions]'
