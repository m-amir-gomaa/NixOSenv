# Build artefacts
result
result-*

# ── Secrets ───────────────────────────────────────────────────────────────────
secrets.nix
# age private key — NEVER commit this
.age-key.txt
# UTCP live config — only templates (.example) are versioned, never the real files
dotfiles/utcp/.env
dotfiles/utcp/igintel-mcp.json

# ── Python / Nix ──────────────────────────────────────────────────────────────
__pycache__/
*.pyc
.direnv/

# ── Sovereign AI / agentic runtime state (local only) ────────────────────────
.swarm/
*.rvf
*.rvf.lock
*.db
CLAUDE.md
claude-flow.config.json
ruflo-sync.py
.stfolder
.stfolder.removed-*
# Claude Code project config — local only (tracked files stay tracked)
.claude/
# Serena AI assistant state — local only
.serena/
# MCP server config — local only (template is .mcp.json.example)
.mcp.json

# ── Accidental media files ────────────────────────────────────────────────────
# These appear when download tools reconstruct absolute paths inside the repo
*.mp3
*.m4a
*.mp4
*.flac

# ── Logs ─────────────────────────────────────────────────────────────────────
nohup.out
*.log

# ── Family PC config (separate flash drive) ──────────────────────────────────
family/

# ── Scratch / test files ─────────────────────────────────────────────────────
scratch/
test-*.conf
*.xlsx
Untitled
test.qml
.zcompdump
